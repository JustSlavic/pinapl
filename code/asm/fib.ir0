
fib:
    mov     r0, 0
    mov     r1, 8
    mov     r2, 0
    mov     r3, 1
.loop:
    cmp     r0, r1
    jeq     .end

    add     r4, r2, r3
    mov     r2, r3
    mov     r3, r4
    mov     r4, 0

    add     r0, r0, 1

    jmp     .loop

.end:

